# Test Plan
Generated by /plan-eng-review on 2026-03-27
Branch: unknown
Repo: local/abstractor

## Affected Pages/Routes
- CLI command `self-fork-lab` — verify installability, argument parsing, and runtime gating for MLX-only execution.
- Module command `python -m consciousness_loop.self_fork_lab` — verify parity with the console script entry point.
- JSON artifact output path — verify one run writes one self-contained trace artifact that can be inspected later.

## Key Interactions to Verify
- Launch a 3-branch run with one prompt and three seeds, then confirm branches advance in lockstep.
- Watch one branch finish early while the others continue, and confirm the UI marks that branch complete without freezing the whole run.
- Run on a narrow terminal width and confirm the renderer falls back to a readable degraded layout.
- Attempt a real run without MLX dependencies and confirm the CLI prints a clear MLX-only error.

## Edge Cases
- Empty prompt should fail fast with a clear validation error.
- `--max-tokens 0` or negative values should fail fast with a clear validation error.
- Backend returns empty continuation on the first step.
- Backend raises during one branch step while other branches are still runnable.
- Artifact output directory does not exist before the run.

## Critical Paths
- Install package -> run `self-fork-lab` -> see 3 live branches -> artifact written.
- Shared branch advancement logic drives both `self_vec_experiment` and `self_fork_lab` consistently.
- Seed-only branching produces diverging responses while shared params remain constant.
