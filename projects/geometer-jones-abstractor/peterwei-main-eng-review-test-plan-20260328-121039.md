# Test Plan
Generated by /plan-eng-review on 2026-03-28
Branch: main
Repo: geometer-jones/abstractor

## Affected Pages/Routes
- `python3 -m consciousness_loop.self_kv_bias_signal --checkpoint <artifact> --n-samples <N>` — verify canonical `research_run` writing, order-swapped judging, band-aware mechanistic verdicts, and slim history indexing.
- `python3 -m consciousness_loop.self_kv_bias_signal --json --checkpoint <artifact>` — verify machine-readable output includes the canonical artifact reference and benchmark verdict.
- `python3 -m unittest tests.test_self_kv_bias_signal tests.test_self_kv_bias_tournament -v` — regression suite for benchmark logic, artifact loading, and history behavior.

## Key Interactions to Verify
- Benchmark a saved fork/cohort artifact and confirm one self-contained `research_run` artifact is written under `~/.abstractor/research/artifacts/`.
- Re-run the same candidate on the same benchmark fingerprint and confirm Elo / strong-signal continuity carries forward.
- Run the same candidate on a different prompt bank or judge model and confirm continuity resets instead of contaminating results.
- Use a judge with second-position bias and confirm order-swapped judging neutralizes the bias in the reported behavioral summary.
- Run with `--no-history` and confirm the canonical artifact is still written while the slim index row is not.

## Edge Cases
- `research_run` artifact missing embedded candidate vector or missing benchmark fingerprint.
- Legacy signal history rows without `artifact_path` or fingerprint fields.
- Candidate artifact loaded with explicit branch / slot overrides.
- Middle-band dominant causal effect that should pass even when late band is not the strongest.
- Early-band-only disruption that should fail the mechanistic verdict despite large divergence.

## Critical Paths
- Saved candidate artifact -> load candidate vector/genome -> benchmark against frozen baseline -> write canonical `research_run` -> append slim history index row.
- Canonical `research_run` -> reload later without the original source artifact -> reproduce the saved verdict and identity metadata.
- Prior benchmark lineage -> new run with the same fingerprint -> correct Elo / strong-signal continuation.
