# Test Plan
Generated by /plan-eng-review on 2026-04-02 00:59:47 PDT
Branch: main
Repo: geometer-jones/the-big-learn

## Affected Pages/Routes
- `the_big_learn` CLI `progress-save` — verify it only saves learner progress artifacts and rejects line-level generated annotations.
- `the_big_learn` CLI new source-line annotation save command — verify a generated annotation saves immediately when a raw-source line is enriched.
- Guided-reading host assets for Codex, Claude, and Gemini — verify all three now instruct immediate answers, no deferred-question storage/display, and the same ritual ordering.
- Raw-source reread path via `python3 -m the_big_learn source read` — verify saved generated annotations appear on reread after line-level save.

## Key Interactions to Verify
- Save a generated annotation for one raw-source line, then rerun `source read` for that chapter and confirm the saved annotation is surfaced.
- Save a line-by-line learner translation log during reading, then save chapter-end `Personal Translation` and `Personal Response` separately.
- Change reading style to `Proactive` or `On-Demand`, then verify the saved style persists without any deferred-question mode.
- Generate/install host assets for Codex, Claude, and Gemini and confirm only allowed host-specific wording differs.

## Edge Cases
- `progress-save` receives `generated_annotations` after the command split.
- Source-line annotation save receives an unknown `line_id`.
- Existing saved progress still contains legacy `question_handling` state.
- Host output accidentally reintroduces deferred-question or fake background-prep wording in one host only.

## Critical Paths
- Raw-source line generation -> immediate generated-annotation save -> later reread shows saved annotation.
- Mid-line learner question -> immediate answer in current line discussion -> return to reading flow, no queue/follow-up phase.
- Shared prompt source -> Codex/Claude/Gemini generated outputs remain aligned.
- Chapter reading -> line translation log persists per line -> chapter-end response persists separately.
