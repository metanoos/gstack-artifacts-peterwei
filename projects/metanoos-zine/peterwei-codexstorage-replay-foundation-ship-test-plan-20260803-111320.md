# Test Plan

Generated by /ship on 2026-08-03
Branch: codex/storage-replay-foundation
Repo: metanoos/zine

## Affected Pages/Routes

- None. This slice changes only the host-independent Rust domain core.

## Key Interactions to Verify

- Reduce linear material histories containing block insertion, Unicode-scalar text edits, rename, and tombstone operations.
- Reduce folder histories containing add, remove, reorder, rename, and tombstone operations.
- Compile resource and historical folder replay from events delivered in different orders.

## Edge Cases

- Reject empty IDs, unsupported schemas, duplicate events, missing dependencies, causal cycles, concurrent heads, and unimplemented merges.
- Reject stale deleted text, no-op authored changes, cross-block ranges, invalid selection offsets, and invalid folder membership frontiers.
- Preserve 64-bit counters as decimal strings across the JSON/JavaScript boundary.

## Critical Paths

- Recorded events -> deterministic dependency order -> exact-frontier reduction -> material or folder projection.
- Historical folder membership -> scoped descendant event replay without copying child events into folder storage.
