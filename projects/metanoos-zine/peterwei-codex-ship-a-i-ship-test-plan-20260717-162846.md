# Test Plan
Generated by /ship on 2026-07-17T16:28:46-07:00
Branch: codex/ship-a-i
Repo: metanoos/zine

## Affected Pages/Routes
- Desktop editor / Action Palette — confirm the declarative AI row preserves the existing one-row labels, order, provider selection, disabled states, stop behavior, and prompt inspection.
- Desktop Models and prompt inspector flow — confirm all five prepared operations still require exact approval and stale responses remain recoverable without applying edits.
- MCP command-line package — install the packed tarball offline in a clean directory and run `zine-mcp --help` from an unrelated working directory.
- Local macOS dogfood bundle — inspect the unsigned current-machine app/DMG for architecture, relay resource placement, unsafe bundled content, and escaped symlinks.

## Key Interactions to Verify
- Prepare, approve, and execute Extend, Settle, Stir, Reply, and Analyze; mutate focus/provider/source between stages and confirm stale approval or output is rejected.
- Start the native provider proxy with supported methods, headers, ordinary and streaming responses; confirm malformed URLs and bounded UTF-8 error messages fail safely.
- Reload hosted and local relay access-policy files while clients are active; confirm last-known-good policy survives corrupt or unreadable updates and deletion returns to documented local mode.
- Verify a Reify trace bundle, including shared target ancestry, exact materialized text, altered events, false conformance declarations, global event-reference limits, and duplicate target event IDs.

## Edge Cases
- Multiple bundle targets share one ordered trace chain; cryptographic conformance runs once while file materialization remains per target.
- A bundle exceeds 10,000 aggregate target-event references; later target indexes are rejected before parsing or conformance.
- A target repeats one valid event ID 10,000 times; one bounded diagnostic is emitted and trace conformance is never invoked.
- Packaged MCP execution has no monorepo, workspace symlink, network access, or caller-working-directory dependency.
- macOS bundle inputs contain wrong-architecture binaries, unexpected artifacts, unsafe state/secrets, dependency trees, source maps, or symlinks escaping the bundle.

## Critical Paths
- Run `npm run verify` under Node 24: protocol corpus, client typecheck and 827 tests, MCP 12 tests/build/package smoke, Go relay tests, Rust 10 tests, macOS tooling tests, production client build, and real-relay protocol smoke all pass.
- Build the shared protocol kernel and exercise the same fixed trace behavior through client, MCP, and real-relay consumers without changing Schema 2, wire format, keys, trust, or ACL semantics.
- Open the desktop editor, select an AI voice/provider, inspect and approve an operation, then apply the exact response only while target state remains current.
